<?xml version="1.0" encoding="iso-8859-1"?>
<!--
 DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS HEADER.
 
 Copyright 1997-2007 Sun Microsystems, Inc. All rights reserved.
 
 The contents of this file are subject to the terms of either the GNU
 General Public License Version 2 only ("GPL") or the Common Development
 and Distribution License("CDDL") (collectively, the "License").  You
 may not use this file except in compliance with the License. You can obtain
 a copy of the License at https://glassfish.dev.java.net/public/CDDL+GPL.html
 or glassfish/bootstrap/legal/LICENSE.txt.  See the License for the specific
 language governing permissions and limitations under the License.
 
 When distributing the software, include this License Header Notice in each
 file and include the License file at glassfish/bootstrap/legal/LICENSE.txt.
 Sun designates this particular file as subject to the "Classpath" exception
 as provided by Sun in the GPL Version 2 section of the License file that
 accompanied this code.  If applicable, add the following below the License
 Header, with the fields enclosed by brackets [] replaced by your own
 identifying information: "Portions Copyrighted [year]
 [name of copyright owner]"
 
 Contributor(s):
 
 If you wish your version of this file to be governed by only the CDDL or
 only the GPL Version 2, indicate your decision by adding "[Contributor]
 elects to include this software in this distribution under the [CDDL or GPL
 Version 2] license."  If you don't indicate a single choice of license, a
 recipient has the option to distribute your version of this file under
 either the CDDL, the GPL Version 2 or to extend the choice of license to
 its licensees as provided above.  However, if you add GPL Version 2 code
 and therefore, elected the GPL Version 2 license, then the option applies
 only if the new code is made subject to such option by the copyright
 holder.
-->

<!DOCTYPE project [
  <!ENTITY glassfish SYSTEM "file:./glassfish.xml">
]>

<project name="Top Level Build" default="all" basedir=".">  
    <property file="${user.home}/build.properties"/>
    <property file="./build.properties"/>
    <property file="./project.properties"/>
    <!-- Do NOT re-arrange the order below -->
    &glassfish;

    <!-- Explode Glassfish installation image under the glassfish.home folder-->
    <target name="bootstrap" unless="exist">
       <mkdir dir="${repo.local}"/>
       <antcall target="get.binaries">
          <param name="name" value="${glassfish.image.name}"/>
          <param name="version" value="${glassfish.image.version}"/>
          <param name="repo.remote3" value="${repo.remote1}"/>
       </antcall>
       <mkdir dir="${glassfish.home}"/>
       <unzip src="${repo.local}/${glassfish.image.name}/jars/${glassfish.jar}" dest="${glassfish.home}"/>
    </target> 

    <target name="get.binaries">
        <echo message="Downloading Binary at: ${repo.local}/jars/${name}-${version}.jar"/>
        <mkdir dir="${repo.local}/${name}/jars"/> 
        <condition property="useProxy">
 	     <and>
                <isset property="maven.proxy.host"/>
                <not>
                    <equals arg1="${maven.proxy.host}" arg2=""/>
                </not>
             </and>	
        </condition>
        <antcall target="set.proxy"/>
        <get src="${repo.remote3}/${name}/jars/${name}-${version}.jar" dest="${repo.local}/${name}/jars/${name}-${version}.jar" usetimestamp="true" />
    </target>

    <target name="set.proxy" if="useProxy">
        <echo message="Setting Proxy Host and Port"/>
        <setproxy proxyhost="${maven.proxy.host}" proxyport="${maven.proxy.port}"/>
    </target>

    <!-- Delete the GlassFish image completely -->
    <target name="clobber">
        <echo>Deleting Glassfish Image under ${glassfish.home}</echo>
        <delete dir="${glassfish.home}"/>
    </target>

    <target name="checkout">
        <echo message="Checking out GlassFish Modules"/>
        <!-- FIXME: change delimiter from "," to " " in dependency.order variables  
        <cvs failonerror="true"  tag="${glassfish.cvstag}"
            dest="${glassfish.root}/${glassfish.cvs.project}">
            <commandline>
                <argument line="-d ${glassfish.cvsroot} update -d ${glassfish.build.dependency.order}"/>
            </commandline>
        </cvs -->

         <cvs cvsRoot="${glassfish.cvsroot}"
                 tag="${glassfish.cvstag}"
                 failonerror="true"
                 package="${glassfish.cvs.project}/activation
                 ${glassfish.cvs.project}/mail
                 ${glassfish.cvs.project}/deployment-api
                 ${glassfish.cvs.project}/transaction-api
                 ${glassfish.cvs.project}/servlet-api
                 ${glassfish.cvs.project}/ejb-api
                 ${glassfish.cvs.project}/persistence-api
                 ${glassfish.cvs.project}/connector-api
                 ${glassfish.cvs.project}/management-api
                 ${glassfish.cvs.project}/appserv-api
                 ${glassfish.cvs.project}/jms-api
                 ${glassfish.cvs.project}/jacc-api
                 ${glassfish.cvs.project}/common-util
                 ${glassfish.cvs.project}/annotation-framework
                 ${glassfish.cvs.project}/appserv-commons
                 ${glassfish.cvs.project}/entity-persistence
                 ${glassfish.cvs.project}/jmx-remote
                 ${glassfish.cvs.project}/admin-core
                 ${glassfish.cvs.project}/jts
                 ${glassfish.cvs.project}/appserv-jstl
                 ${glassfish.cvs.project}/pwc-commons
                 ${glassfish.cvs.project}/appserv-webtier
                 ${glassfish.cvs.project}/webtier-extensions
                 ${glassfish.cvs.project}/jacc-provider
                 ${glassfish.cvs.project}/container-auth
                 ${glassfish.cvs.project}/appserv-core
                 ${glassfish.cvs.project}/appserv-http-engine
                 ${glassfish.cvs.project}/appserv-docs
                 ${glassfish.cvs.project}/admin
                 ${glassfish.cvs.project}/admin-cli
                 ${glassfish.cvs.project}/appserv-addons
                 ${glassfish.cvs.project}/jdbcra
                 ${glassfish.cvs.project}/cmp
                 ${glassfish.cvs.project}/tools
                 ${glassfish.cvs.project}/avk
                 ${glassfish.cvs.project}/admin-gui
                 ${glassfish.cvs.project}/appserv-native"
                 dest="${glassfish.root}"/>
    </target>

    <target name="nbuild">
        <!-- buildlist reference="my-build-path">
          <fileset dir="${glassfish.root}/glassfish" includes="*/build.xml"/>
        </buildlist>
        <subant target="build">
          <buildpath refid="my-build-path"/>
        </subant -->
    </target>

    <target name="nclean">
    </target>

    <!-- Bootstrap binaries required for building from source-->
    <target name="bootstrap-all" unless="exist">
       <mkdir dir="${repo.local}"/>
       <antcall target="download-bootstrap-all"/>
       <antcall target="post-bootstrap"/>
    </target> 

    <!-- Configure GlassFish Server Image -->
    <target name="configure-runtime" unless="exist">
       <copy file="${glassfish.maven}/setup.xml" todir="${glassfish.home}" overwrite="true"/>
       <ant dir="${glassfish.home}" antfile="setup.xml" target="all"/>
    </target> 

    <target name="download-bootstrap-all">
       <antcall target="get.binaries">
          <param name="name" value="${glassfish.ant.name}"/>
          <param name="version" value="${glassfish.ant.version}"/>
       </antcall>
       <antcall target="get.binaries">
          <param name="name" value="${glassfish.commons_beanutils.name}"/>
          <param name="version" value="${glassfish.commons_beanutils.version}"/>
       </antcall>
       <antcall target="get.binaries">
          <param name="name" value="${glassfish.commons_collections.name}"/>
          <param name="version" value="${glassfish.commons_collections.version}"/>
       </antcall>
       <antcall target="get.binaries">
          <param name="name" value="${glassfish.commons_digester.name}"/>
          <param name="version" value="${glassfish.commons_digester.version}"/>
       </antcall>
       <antcall target="get.binaries">
          <param name="name" value="${glassfish.commons_launcher.name}"/>
          <param name="version" value="${glassfish.commons_launcher.version}"/>
       </antcall>
       
       <antcall target="get.binaries">
          <param name="name" value="${glassfish.commons_logging.name}"/>
          <param name="version" value="${glassfish.commons_logging.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.commons_modeler.name}"/>
          <param name="version" value="${glassfish.commons_modeler.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.jdbcrajars.name}"/>
          <param name="version" value="${glassfish.jdbcrajars.version}"/>
       </antcall>
       
       <antcall target="get.binaries">
          <param name="name" value="${glassfish.jakarta_regexp.name}"/>
          <param name="version" value="${glassfish.jakarta_regexp.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.javahelp.name}"/>
          <param name="version" value="${glassfish.javahelp.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.jsf.name}"/>
          <param name="version" value="${glassfish.jsf.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.netbeans.name}"/>
          <param name="version" value="${glassfish.netbeans.version}"/>
       </antcall>
     
       <antcall target="get.binaries">
          <param name="name" value="${glassfish.netbeans.name}"/>
          <param name="version" value="${glassfish.netbeans_dbschema.version}"/>
       </antcall>
      
       <antcall target="get.binaries">
          <param name="name" value="${glassfish.jaxr_ra.name}"/>
          <param name="version" value="${glassfish.jaxr_ra.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.rjmx_ri.name}"/>
          <param name="version" value="${glassfish.rjmx_ri.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.peorb.name}"/>
          <param name="version" value="${glassfish.peorb.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.ldapbp.name}"/>
          <param name="version" value="${glassfish.ldapbp.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.jbi_impl.name}"/>
          <param name="version" value="${glassfish.jbi_impl.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.derby.name}"/>
          <param name="version" value="${glassfish.derby.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.genericra.name}"/>
          <param name="version" value="${glassfish.genericra.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.jsr250_api.name}"/>
          <param name="version" value="${glassfish.jsr250_api.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.installer_builder.name}"/>
          <param name="version" value="${glassfish.installer_builder.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.webservices_api.name}"/>
          <param name="version" value="${glassfish.webservices_api.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.webservices_extra_api.name}"/>
          <param name="version" value="${glassfish.webservices_extra_api.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.webservices_rt.name}"/>
          <param name="version" value="${glassfish.webservices_rt.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.webservices_tools.name}"/>
          <param name="version" value="${glassfish.webservices_tools.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.xwss_config.name}"/>
          <param name="version" value="${glassfish.xwss_config.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.wstx_services.name}"/>
          <param name="version" value="${glassfish.wstx_services.version}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.imq.name}"/>
          <param name="version" value="${glassfish.imq.version}"/>
          <param name="repo.remote3" value="${repo.remote2}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.appserv_native.name}"/>
          <param name="version" value="${glassfish.appserv_native.version}"/>
          <param name="repo.remote3" value="${repo.remote2}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.nss_util.name}"/>
          <param name="version" value="${glassfish.nss_util.version}"/>
          <param name="repo.remote3" value="${repo.remote2}"/>
       </antcall>

       <antcall target="get.binaries">
          <param name="name" value="${glassfish.NSsecurity.name}"/>
          <param name="version" value="${glassfish.NSsecurity.version}"/>
          <param name="repo.remote3" value="${repo.remote2}"/>
       </antcall>
    </target> 

</project>
