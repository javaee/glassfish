<?xml version="1.0" encoding="ISO-8859-1"?>

<!DOCTYPE project [
<!ENTITY commonSetup SYSTEM "file:./../../../../../config/properties.xml">
<!ENTITY commonBuild SYSTEM "file:./../../../../../config/common.xml">
<!ENTITY testproperties SYSTEM "file:./build.properties">
]>

<project name="ejb-ejb31-security-simple-App" default="usage" basedir=".">

    &commonSetup;
    &commonBuild;
    &testproperties;

    <target name="all" depends="build,create-user,deploy,run,undeploy,delete-user"/>

    <target name="clean" depends="init-common">
      <antcall  target="clean-common"/>
    </target>
 
    <target name="compile" depends="clean">
        <antcall target="compile-common">
            <param name="src" value="ejb"/>
        </antcall>
        <antcall target="compile-common">
            <param name="src" value="client"/>
        </antcall>
    </target>

    <target name="create-user">
      <antcall target="create-user-common">
            <param name="user" value="bob"/>
            <param name="password" value="cob"/>
            <param name="groups" value="employee"/>
       </antcall>
   </target>

    <target name="delete-user">
       <antcall target="delete-user-common">
            <param name="user" value="bob"/>
      </antcall>
    </target>

    <target name="build" depends="compile">
        <antcall target="ejb-jar-common">
            <param name="ejbjar.classes" 
              value="**/Singleton*.class,**/Hello*.class"/>
        </antcall>
        <antcall target="appclient-jar-common">
            <param name="appclientjar.classes" 
              value="**/Client.class,**/Hello.class"/>
        </antcall>
        <echo message="appclient.jar=${assemble.dir}/${appname}-client.jar"/>
        <jar jarfile="${assemble.dir}/${appname}-client.jar" update="yes">
            <zipfileset src="${env.APS_HOME}/lib/reporter.jar" 
                        excludes="META-INF/**"/>
        </jar>

    </target> 
    
    <target name="deploy" 
            depends="init-common">
        <antcall target="deploy-jar-common"/>
    </target>

    <target name="run" depends="init-common">
        <echo message="appclient -client ${assemble.dir}/${appname}-clientClient.jar -name ${appname}Client -textauth -user j2ee -password j2ee -xml ${env.S1AS_HOME}/domains/${admin.domain}/config/sun-acc.xml ${appclient.application.args}"/>   
        <exec executable="${APPCLIENT}" failonerror="false">
<!-- Don't deploy client -->
            <arg line="-client
                 ${assemble.dir}/${appname}-clientClient.jar"/>
            <arg line="-client ${assemble.dir}/${appname}-client.jar"/>
            <arg line="-name ${appname}Client"/>
            <arg line="-textauth"/>
            <arg line="-user bob"/>
            <arg line="-password cob"/>
            <arg line="-xml ${env.S1AS_HOME}/domains/${admin.domain}/config/sun-acc.xml"/>
            <arg line="${jndiroot}"/>
        </exec>

    </target>
 
    <target name="run_se" depends="init-common">
       <java  fork="on" 
              failonerror="true"
              classpath="${env.S1AS_HOME}/lib/javaee.jar:${env.S1AS_HOME}/modules/gf-client.jar:${build.classes.dir}:${env.APS_HOME}/lib/reporter.jar"
              classname="${se.client}">
           <arg line="${jndiroot}"/>
        </java>
    </target>

    <target name="undeploy" depends="init-common">
        <antcall target="undeploy-jar-common"/>
    </target>   

    <target name="usage">
        <antcall target="usage-common"/>
    </target>
</project>
