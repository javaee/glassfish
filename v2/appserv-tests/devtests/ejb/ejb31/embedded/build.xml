<?xml version="1.0" encoding="ISO-8859-1"?>

<!DOCTYPE project [
<!ENTITY commonSetup SYSTEM "file:./../../../../config/properties.xml">
<!ENTITY commonBuild SYSTEM "file:./../../../../config/common.xml">
<!ENTITY testproperties SYSTEM "file:./build.properties">
]>

<project name="ejb-ejb31-embedded-App" default="usage" basedir=".">

    &commonSetup;
    &commonBuild;
    &testproperties;

    <target name="all" depends="build,run"/>

    <target name="clean" depends="init-common">
      <antcall  target="clean-common"/>
    </target>
 
    <target name="compile" depends="clean">
        <antcall target="compile-common">
            <param name="src" value="ejb"/>
        </antcall>
        <antcall target="compile-common">
            <param name="src" value="client"/>
        </antcall>
    </target>

    <target name="build" depends="compile">
        <!-- package ourselves to include persistence.xml -->
        <mkdir dir="${assemble.dir}"/>
        <jar jarfile="${assemble.dir}/${appname}-ejb.jar" 
             basedir="${build.classes.dir}" 
             update="true"
             includes ="**/S*.class">
             <metainf dir="descriptor">
                  <include name="ejb-jar.xml"/>
                  <include name="persistence.xml"/>
             </metainf> 
        </jar>
    </target> 
    
    <target name="run" depends="init-common">
        <antcall target="run_se"/>
    </target>
 
    <target name="run_se" depends="init-common">
       <java  fork="on" 
              failonerror="true"
              classpath="${assemble.dir}/${appname}-ejb.jar:${env.S1AS_HOME}/lib/embedded/glassfish-embedded-static-shell.jar:${build.classes.dir}:${env.APS_HOME}/lib/reporter.jar"
              classname="${se.client}">
           <arg line="${jndiroot}"/>
        </java>
    </target>

    <target name="usage">
        <antcall target="usage-common"/>
    </target>
</project>
