<?xml version="1.0" encoding="ISO-8859-1"?>

<!DOCTYPE project [
<!ENTITY commonSetup SYSTEM "file:./../../../config/properties.xml">
<!ENTITY commonBuild SYSTEM "file:./../../../config/common.xml">
<!ENTITY run SYSTEM "file:./../../../config/run.xml">
<!ENTITY testproperties SYSTEM "file:./build.properties">
]>

<project name="webcontainer_unittest" default="all" basedir=".">

    &commonSetup;
    &commonBuild;
    &testproperties;
    &run;

    <target name="all" depends="setup,restart-server-instance-common,run, restore,restart-server-instance-common"/>

    <target name="setup" depends="init-common">
        <antcall target="asadmin-common-ignore-fail">
            <param name="admin.command" value="set" />
            <param name="operand.props"
                   value="${appserver.instance.name}.http-service.keep-alive.timeout-in-seconds=0"/>
        </antcall>
    </target>

    <target name="restore" depends="init-common">
        <antcall target="asadmin-common-ignore-fail">
            <param name="admin.command" value="set" />
            <param name="operand.props"
                   value="${appserver.instance.name}.http-service.keep-alive.timeout-in-seconds=60"/>
        </antcall>
    </target>

    <target name="run" depends="init-common">
        <javac srcdir="." classpath="${env.APS_HOME}/lib/reporter.jar"
               includes="WebTest.java"/>
        <java classname="WebTest">
          <arg value="${http.host}"/>
          <arg value="${http.port}"/>
          <arg value="${contextroot}"/>
          <classpath>
           <pathelement location="${env.APS_HOME}/lib/reporter.jar"/>
           <pathelement location="."/>
         </classpath>
        </java>
    </target>
 
    <target name="usage">
        <antcall target="usage-common"/>
    </target>
</project>
