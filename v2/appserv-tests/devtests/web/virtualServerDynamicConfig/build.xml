<?xml version="1.0" encoding="ISO-8859-1"?>

<!DOCTYPE project [
<!ENTITY commonSetup SYSTEM "./../../../config/properties.xml">
<!ENTITY commonBuild SYSTEM "./../../../config/common.xml">
<!ENTITY testproperties SYSTEM "./build.properties">
]>

<project name="webcontainer_unittest" default="all" basedir=".">

    &commonSetup;
    &commonBuild;
    &testproperties;

    <target name="all" depends="build,create-virtual-server,create-http-listener,deploy,run,undeploy,delete-http-listener,delete-virtual-server"/>

    <target name="create-virtual-server" depends="init-common">
        <exec executable="${ASADMIN}">
            <arg line="create-virtual-server"/>
            <arg line="-u ${admin.user}"/>
            <arg line="--port ${admin.port}"/>
            <arg line="--hosts localhost" />
            <arg line="server3" />
        </exec>
    </target>

    <target name="create-http-listener" depends="init-common">
        <exec executable="${ASADMIN}">
            <arg line="create-http-listener"/>
            <arg line="-u ${admin.user}"/>
            <arg line="--port ${admin.port}"/>
            <arg line="--listeneraddress 0.0.0.0" />
            <arg line="--listenerport 8100" />
            <arg line="--defaultvs server3" />
            <arg line="--servername localhost.localdomain" />
            <arg line="--acceptorthreads 10" />
            <arg line="http-listener-server3" />
        </exec>
    </target>

    <target name="delete-virtual-server" depends="init-common">
        <exec executable="${ASADMIN}">
            <arg line="delete-virtual-server"/>
            <arg line="-u ${admin.user}"/>
            <arg line="--port ${admin.port}"/>
            <arg line="server3" />
        </exec>
    </target>

    <target name="delete-http-listener" depends="init-common">
        <exec executable="${ASADMIN}">
            <arg line="delete-http-listener"/>
            <arg line="-u ${admin.user}"/>
            <arg line="--port ${admin.port}"/>
            <arg line="http-listener-server3" />
        </exec>
    </target>

    <target name="clean" depends="init-common">
      <antcall target="clean-common"/>
    </target>
       
    <target name="compile" depends="clean">
        <antcall target="compile-common">
            <param name="src" value="servlet"/>
        </antcall>
    </target>

    <target name="build" depends="compile">
        <property name="hasWebclient" value="yes"/> 
        <antcall target="webclient-war-common">
            <param name="hasWebclient" value="yes"/>
            <param name="webclient.war.classes" value="**/*.class"/> 
        </antcall>

         <javac
            srcdir="."
            classpath="${env.APS_HOME}/lib/reporter.jar"
            includes="WebTest.java"/>

    </target> 

    <target name="build-publish-war" depends="build, publish-war-common" />
    
    <target name="re-deploy" depends="init-common">
        <antcall target="deploy-war-common"/>
    </target>

    <target name="deploy" depends="init-common">
        <antcall target="deploy-war-common"/>
    </target>
    
    <target name="run" depends="init-common">
        <java classname="WebTest">
          <arg value="${http.host}"/>
          <arg value="${http.port}"/>
          <arg value="${contextroot}"/>
          <classpath>
           <pathelement location="${env.APS_HOME}/lib/reporter.jar"/>
           <pathelement location="."/>
         </classpath>
        </java>
    </target>
 
    <target name="undeploy" depends="init-common">
        <antcall target="undeploy-war-common"/>
    </target>   

    <target name="usage">
        <antcall target="usage-common"/>
    </target>
</project>
