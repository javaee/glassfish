<?xml version="1.0" encoding="iso-8859-1"?>
<!--
The contents of this file are subject to the terms
of the Common Development and Distribution License
(the "License").  You may not use this file except
in compliance with the License.

You can obtain a copy of the license at
glassfish/bootstrap/legal/CDDLv1.0.txt or
https://glassfish.dev.java.net/public/CDDLv1.0.html.
See the License for the specific language governing
permissions and limitations under the License.

When distributing Covered Code, include this CDDL
HEADER in each file and include the License file at
glassfish/bootstrap/legal/CDDLv1.0.txt.  If applicable,
add the following below this CDDL HEADER, with the
fields enclosed by brackets "[]" replaced with your
own identifying information: Portions Copyright [yyyy]
[name of copyright owner]
-->

<project name="glassfish-embedded-nucleus" default="create.distribution" basedir=".">
    <property name="rootdir" value="target"/>
    <property name="zipdir" value="${rootdir}/packager_zips"/>
    <property name="jardir" value="${rootdir}/packager_jars"/>
    <property name="filedir" value="${rootdir}/packager_files"/>
    <property name="finaljar" value="${rootdir}/final.jar"/>

    <target name="create.distribution">

        <!-- The GFE Embedded API will be a jar file -->
        <copy todir="${jardir}">
            <fileset dir="${zipdir}">
                <include name="*.jar"/>
            </fileset>
        </copy>
        
        <!-- The packager files are zips with jars inside -->
        <unzip dest="${jardir}">
            <fileset dir="${zipdir}">
                <include name="*.zip"/>
            </fileset>
                
            <!-- This does not work when called from maven.  Very annoying!
            <mapper type="flatten"/-->
        </unzip>

        <echo message="Exploding the jar files..."/>
        <unjar dest="${filedir}">
            <fileset dir="${jardir}">
                <include name="**/*.jar"/>
            </fileset>
        </unjar>
        
        <echo message="Re-packaging the files..."/>
        <delete file="${finaljar}"/>
        <jar destfile="${finaljar}" duplicate="fail" basedir="${filedir}"/>
        
        <attachArtifact file="${finaljar}"/>
   </target>
</project>
