<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.glassfish.distributions</groupId>
        <artifactId>distributions</artifactId>
        <version>10.0-SNAPSHOT</version>
    </parent>    
    <artifactId>web</artifactId>
    <version>${pe.version}</version>
    <name>Glassfish PE Bundle</name>
    <packaging>distribution-base-zip</packaging>
    <description>This pom describes how to assemble the Glassfish PE Bundle</description>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jvnet.maven-antrun-extended-plugin</groupId>
                <artifactId>maven-antrun-extended-plugin</artifactId>
                <configuration>
                  <tasks>
                    <ant dir="." antfile="build.xml" target="create.bundle"/>
                  </tasks>
                </configuration>
             </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>javadb</groupId>
            <artifactId>javadb</artifactId>
            <version>10.2.2.1-20070823</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.common</groupId>
            <artifactId>dol</artifactId>
            <version>${pe.version}</version>
        </dependency>        
        <dependency>
            <groupId>org.glassfish.web</groupId>
            <artifactId>war-util</artifactId>
            <version>${pe.version}</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.ejb</groupId>
            <artifactId>gf-ejb-connector</artifactId>
            <version>${pe.version}</version>
        </dependency>        
        <dependency>
            <groupId>org.glassfish.web</groupId>
            <artifactId>gf-web-connector</artifactId>
            <version>${pe.version}</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.extras</groupId>
            <artifactId>gf-jruby-connector</artifactId>
            <version>${pe.version}</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.extras</groupId>
            <artifactId>gf-phobos-connector</artifactId>
            <version>${pe.version}</version>
        </dependency>
        <dependency>
            <groupId>javax.javaee</groupId>
            <artifactId>javaee</artifactId>
            <version>${javaee-api.version}</version>
        </dependency>
        <!-- base distribution image -->
        <dependency>
            <groupId>org.glassfish.distributions</groupId>
            <artifactId>nucleus</artifactId>
            <version>${nucleus.version}</version>
            <type>zip</type>
        </dependency>
        
        
        <dependency>
            <groupId>org.glassfish.common</groupId>
            <artifactId>amx-impl</artifactId>
            <version>${pe.version}</version>
            <type>zip</type>
        </dependency>

    </dependencies>
</project>

