war.file=jpaservlet
testsuite.name=jpaweb
testng.test.name=jpa_servlet_finder
contextroot=persistence
#possible choices war|ear|jar
archive.type=war
#specify only if running locally.Path to ${workspace_root}/test-output
ws.root=../../

    

#Definitely need to set this at local level
deploy.platform=v3
#deploy.url=http://${deploy.server}:${deploy.port}/
#http://${admin.host}:${admin.port}/__asadmin/deploy?path=<path>;name=<name>
