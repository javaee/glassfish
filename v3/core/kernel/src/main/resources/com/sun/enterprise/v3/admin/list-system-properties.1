
asadmin Utility Subcommands             list-system-properties(1)

NAME
     list-system-properties - lists the system properties of  the
     domain

SYNOPSIS
     list-system-properties
     [--help]
     [ target]

DESCRIPTION
     This command lists the system properties of a domain.

OPTIONS
     --help

         Displays the help text for the subcommand.

OPERANDS
     target
         Do not specify this option. This option is retained  for
         compatibility  with  other releases. If you specify this
         option, a syntax error does not occur. Instead, the sub-
         command  runs  successfully  and  the option is silently
         ignored.

EXAMPLES
     Example 1 Using list-system-properties

       asadmin> list-system-properties --user admin
       --passwordfile password.txt --host localhost --port 4848
       http-listener-port=1088 mycluster
       http-listener-port=1088
       Command list-system-properties executed successfully.

EXIT STATUS
     0                           command executed successfully

     1                           error in executing the command

SEE ALSO
     create-system-properties(1), delete-system-property(1)

Java EE 6           Last change: 23 Sep 2008                    1

