<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <parent>
        <groupId>org.glassfish.core</groupId>
        <artifactId>core</artifactId>
        <version>10.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>glassfish</artifactId>
    <packaging>hk2-jar</packaging>
    <name>Appserver Core Bootstraping Classes</name>
    <description>Glassfish appserver boostraping classes</description>

    <developers>
        <developer>
            <id>dochez</id>
            <name>Jerome Dochez</name>
            <url>http://blogs.sun.com/dochez</url>
            <organization>Sun Microsystems, Inc.</organization>
            <roles>
                <role>lead</role>
                <role>developer</role>
            </roles>
        </developer>
    </developers>    
    <build>
        <plugins>
            <plugin>
                <groupId>com.sun.enterprise</groupId>
                <artifactId>hk2-maven-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>com.sun.enterprise.glassfish.bootstrap.Main</mainClass>
                        </manifest>
                        <!-- this specifies the bootstrap module -->
                        <manifestEntries>
                            <HK2-Bundle-Name>glassfish-bootstrap</HK2-Bundle-Name>
                            <HK2-Repositories>shared lib</HK2-Repositories>
                            <HK2-Repository-shared>uri=. weight=35 type=directory</HK2-Repository-shared>
                            <HK2-Repository-lib>uri=../lib weight=10 type=directory</HK2-Repository-lib>
                            <HK2-Main-Bundle>org.glassfish.core:kernel</HK2-Main-Bundle>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
    <dependencies>
        <dependency>
            <!-- can't depend on com.sun.enterprise:hk2 because that would remove hk2 jars from Class-Path, so our Main class will fail to load. -->
            <groupId>com.sun.enterprise</groupId>
            <artifactId>hk2-core</artifactId>
            <version>${hk2.version}</version>
        </dependency>
        <dependency>
            <groupId>com.sun.xml.stream</groupId>
            <artifactId>sjsxp</artifactId>
            <version>1.0</version>
        </dependency>
    </dependencies>
</project>

