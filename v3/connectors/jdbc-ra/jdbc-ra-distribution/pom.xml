<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <parent>
    <artifactId>jdbc-ra</artifactId>
    <groupId>org.glassfish.connectors.jdbc-ra</groupId>
    <version>10.0-SNAPSHOT</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.glassfish.connectors.jdbc-ra.jdbcra-bundler</groupId>
  <artifactId>jdbcra-bundler</artifactId>
  <name>jdbcra-bundler</name>
  <packaging>distribution-fragment</packaging>

  <build>
    <plugins>

      <!--plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>ds_unpack</id>
            <phase>package</phase>
            <goals>
              <goal>unpack</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>org.glassfish.connectors.jdbc-ra.jdbc-core</groupId>
                  <artifactId>jdbc-core</artifactId>
                  <version>${project.parent.version}</version>
                </artifactItem>
                <artifactItem>
                  <groupId>org.glassfish.connectors.jdbc-ra.jdbc40</groupId>
                  <artifactId>jdbc40</artifactId>
                  <version>${project.parent.version}</version>
                </artifactItem>
              </artifactItems>
              <includes>**/*.class,**/*.properties</includes>
              <excludes>
                **/XAManagedConnectionFactory.class,**/CPManagedConnectionFactory.class,**/DMManagedConnectionFactory.class
              </excludes>
              <outputDirectory>target/__ds_archive</outputDirectory>
            </configuration>
          </execution>
          <execution>
            <id>cp_unpack</id>
            <phase>package</phase>
            <goals>
              <goal>unpack</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>org.glassfish.connectors.jdbc-ra.jdbc-core</groupId>
                  <artifactId>jdbc-core</artifactId>
                  <version>${project.parent.version}</version>
                </artifactItem>
                <artifactItem>
                  <groupId>org.glassfish.connectors.jdbc-ra.jdbc40</groupId>
                  <artifactId>jdbc40</artifactId>
                  <version>${project.parent.version}</version>
                </artifactItem>
              </artifactItems>
              <includes>**/*.class,**/*.properties</includes>
              <excludes>
                **/XAManagedConnectionFactory.class,**/DSManagedConnectionFactory.class,**/DMManagedConnectionFactory.class
              </excludes>
              <outputDirectory>target/__cp_archive</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <executions>
          <execution>
            <id>ds_build-jar</id>
            <phase>package</phase>
            <goals>
              <goal>jar</goal>
            </goals>
            <configuration>
              <classesDirectory>target/__ds_archive</classesDirectory>
              <finalName>ds</finalName>
              <outputDirectory>target/__ds_jar</outputDirectory>
              <includes>
                <include>**/*.class</include>
                <include>**/LocalStrings.properties</include>
              </includes>
            </configuration>
          </execution>
          <execution>
            <id>cp_build-jar</id>
            <phase>package</phase>
            <goals>
              <goal>jar</goal>
            </goals>
            <configuration>
              <classesDirectory>target/__cp_archive</classesDirectory>
              <finalName>cp</finalName>
              <outputDirectory>target/__cp_jar</outputDirectory>
              <includes>
                <include>**/*.class</include>
                <include>**/LocalStrings.properties</include>
              </includes>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-rar-plugin</artifactId>
        <executions>
          <execution>
            <id>ds_build-rar</id>
            <phase>package</phase>
            <goals>
              <goal>rar</goal>
            </goals>
            <configuration>
              <finalName>jdbc-ra_ds</finalName>
              <manifestFile>xml/META-INF/MANIFEST.MF</manifestFile>
              <raXmlFile>xml/ds/ra.xml</raXmlFile>
              <rarSourceDirectory>target/__ds_jar</rarSourceDirectory>
              <includeJar>false</includeJar>
              <workDirectory>target/__ds_tmp</workDirectory>
            </configuration>
          </execution>
          <execution>
            <id>cp_build-rar</id>
            <phase>package</phase>
            <goals>
              <goal>rar</goal>
            </goals>
            <configuration>
              <finalName>jdbc-ra_cp</finalName>
              <manifestFile>xml/META-INF/MANIFEST.MF</manifestFile>
              <raXmlFile>xml/cp/ra.xml</raXmlFile>
              <rarSourceDirectory>target/__cp_jar</rarSourceDirectory>
              <includeJar>false</includeJar>
              <workDirectory>target/__cp_tmp</workDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin-->

      <plugin>
        <groupId>org.jvnet.maven-antrun-extended-plugin</groupId>
        <artifactId>maven-antrun-extended-plugin</artifactId>
        <configuration>
          <tasks>

            <resolveArtifact artifactId="jdbc-core" property="core.jar"/>
            <resolveArtifact artifactId="jdbc30" property="40.jar"/>
            <resolveArtifact artifactId="jdbc40" property="30.jar"/>

            <mkdir dir="target/stage/glassfish/lib/install/applications" />
            <unjar src="${30.jar}" destdir="target/stage" />

            <!-- TODO: to create jars, rars -->

            <echo file="target/stage/glassfish/lib/install/applications/test.txt">JDBC RA</echo>
            <zip file="jdbcra.zip" dir="target"/>

            <!-- TODO: Kohsuke to come back here and make sure mvn install puts a zip file, not .distribution-fragment -->
            <attachArtifact file="jdbcra.zip" />

            <!-- TODO: probably unnecessary -->
            <!--
            <delete dir="target/__ds_archive"/>
            <delete dir="target/__ds_jar"/>
            <delete dir="target/__cp_archive"/>
            <delete dir="target/__cp_jar"/>
            -->
          </tasks>
        </configuration>
      </plugin>
    </plugins>
    <extensions>
      <extension>
          <groupId>org.glassfish.build</groupId>
          <artifactId>maven-glassfish-extension</artifactId>
          <version>1.0-SNAPSHOT</version>
      </extension>
    </extensions>
  </build>

  <dependencies>

    <dependency>
      <groupId>org.glassfish.connectors.jdbc-ra.jdbc-core</groupId>
      <artifactId>jdbc-core</artifactId>
      <version>${project.parent.version}</version>
    </dependency>

    <!--dependency>
      <groupId>org.glassfish.connectors.jdbc-ra.jdbc40</groupId>
      <artifactId>jdbc40</artifactId>
      <version>${project.parent.version}</version>
    </dependency>

    <dependency>
      <groupId>org.glassfish.connectors.jdbc-ra.jdbc30</groupId>
      <artifactId>jdbc30</artifactId>
      <version>${project.parent.version}</version>
    </dependency-->

  </dependencies>
</project>
