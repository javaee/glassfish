
asadmin Utility Subcommands                     start-instance(1)

NAME
     start-instance - starts a server instance

SYNOPSIS
     start-instance
     [--help]
      instance_name

DESCRIPTION
     Note -

       This command is available only in domains that are config-
       ured to support clusters, such as domains that are created
       with the cluster profile or the  enterprise  profile.  For
       information about profiles, see Usage Profiles in Sun Java
       System Application Server 9.1 Administration Guide.

     This command starts an instance with the instance  name  you
     specify.

OPTIONS
     --help                      Displays the help text  for  the
     -?                          subcommand.

OPERANDS
     instance_name               This is the name of  the  server
                                 instance to start.

EXAMPLES
     Example 1 Using start-instance

       asadmin> start-instance -- instance_name instance1
       Instance instance1 started

EXIT STATUS
     0    command executed successfully

     1    error in executing the command

INTERFACE EQUIVALENT
     Server Instance page

SEE ALSO

Java EE 6           Last change: 13 Jul 2007                    1

asadmin Utility Subcommands                     start-instance(1)

     delete-instance(1),  create-instance(1),   stop-instance(1),
     start-appserv(1),   stop-appserv(1),  start-domain(1),.stop-
     domain(1)

Java EE 6           Last change: 13 Jul 2007                    2

