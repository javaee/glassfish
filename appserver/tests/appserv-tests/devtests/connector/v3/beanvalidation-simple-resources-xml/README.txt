This test is same as "beanvalidation-simple", but using glassfish-resources.xml to define the resources

Refer @Readme across the test for comments

1) RA bean is annotated with bean-validations and also has a validation-mapping.xml
2) AdminObject & AdminObject1 are annotated with bean-validations and also has a validation-mapping.xml
3) ActivationSpec is annotated

